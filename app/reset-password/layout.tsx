import type { Metadata } from "next";
export const metadata: Metadata = { title: "Set a New Password | Showwork", description: "Set a new password for your Showwork account.", robots: { index: false, follow: false, noarchive: true } };
export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) { return children; }
