import type { Metadata } from "next";
export const metadata: Metadata = { title: "Workspace Invitation | Showwork", description: "Accept an invitation to collaborate in a Showwork content workspace.", robots: { index: false, follow: false, noarchive: true } };
export default function CalendarInvitesLayout({ children }: { children: React.ReactNode }) { return children; }
