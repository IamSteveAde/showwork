import type { Metadata } from "next";
export const metadata: Metadata = { title: "Reset Your Password | Showwork", description: "Request a secure password reset for your Showwork account.", robots: { index: false, follow: false, noarchive: true } };
export default function ForgotPasswordLayout({ children }: { children: React.ReactNode }) { return children; }
